machine:
  python:
    version: 2.7.5

dependencies:
  pre:
    - sudo apt-get install fabric
deployment:
    production:
      branch: master
      commands:
        - fab run_highstate_prod
    dev:
      branch: dev
      commands:
        - fab run_highstate_dev
