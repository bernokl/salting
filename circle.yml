machine:
  python:
    version: 2.7.5

dependencies:
  pre:
    - sudo apt-get install fabric
test:
  pre:
    - fab test_run_highstate_dev
deployment:
    production:
      branch: master
      commands:
        - fab run_highstate_prod
    dev:
      brach: dev
      commands:
        - fab run_highstate_dev
