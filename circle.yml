machine:
  python:
    version: 2.7.5

dependencies:
  pre:
    - sudo apt-get install fabric
test:
  pre:
    - fab test_run_highstate
deployment:
    production:
      branch: master
      commands:
        - fab run_highstate
